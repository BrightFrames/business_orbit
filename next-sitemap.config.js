/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://businessorbit.org',
    generateRobotsTxt: true,
    // Optional: Add any other configuration here
    // exclude: ['/secret/*'],
}
